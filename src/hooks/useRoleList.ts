//import { useGetUsers } from "@/hooks/apis/useUserManagement";

//export function useRoleList() {
//const { data: allUserList } = useGetUsers(1, 1000, '0', '0', '0', '0', '0');
//BE role 값 추가 필요
//const roleList = Array.from(new Set(allUserList?.data?.map((user) => user.role)));

//return roleList;
//}
